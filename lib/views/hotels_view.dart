import 'package:flutter/material.dart';
import 'package:travellers_log/widgets/hotel_where.dart';

class HotelsView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: HotelWhere(),
    );
  }
}
